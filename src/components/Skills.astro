---
const skills = [
  "Angular", "Flutter", "React", "TypeScript", "Dart", "Python", "Node.js", 
  "Tailwind CSS", "Git", "Docker", "Figma", "Sass", "RxJS", "Next.js", "Firebase", "Astro"
];
---

<section id="skills" class="py-20 overflow-hidden">
  <div class="mb-12 text-center">
    <h2 class="text-3xl font-bold text-white md:text-4xl">Tecnologías</h2>
    <p class="mt-4 text-gray-400">Herramientas que uso para dar vida a mis ideas.</p>
  </div>

  <div class="relative w-full overflow-hidden">
    <!-- Gradient Masks -->
    <div class="absolute left-0 top-0 z-10 h-full w-24 bg-gradient-to-r from-background to-transparent"></div>
    <div class="absolute right-0 top-0 z-10 h-full w-24 bg-gradient-to-l from-background to-transparent"></div>
    
    <!-- Marquee Container -->
    <div class="flex w-max animate-marquee gap-8 py-4">
      {/* Duplicate list for seamless loop */}
      {[...skills, ...skills, ...skills].map((skill) => (
        <div class="flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-6 py-4 transition-colors hover:border-accent/50 hover:bg-white/10 group cursor-default">
          <span class="text-lg font-bold text-gray-400 group-hover:text-white transition-colors">
            {skill}
          </span>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  @keyframes marquee {
    from { transform: translateX(0); }
    to { transform: translateX(-33.33%); }
  }
  
  .animate-marquee {
    animation: marquee 30s linear infinite;
  }
  
  .animate-marquee:hover {
    animation-play-state: paused;
  }
</style>
