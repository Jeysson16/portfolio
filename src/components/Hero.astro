---
---

<section class="relative flex min-h-[80vh] flex-col-reverse items-center justify-center gap-10 py-10 md:flex-row md:justify-between md:py-20">
  <div class="flex flex-col items-center gap-6 text-center md:items-start md:text-left">
    <div class="space-y-2">
      <span class="inline-block rounded-full bg-accent/10 px-3 py-1 text-sm font-medium text-accent ring-1 ring-inset ring-accent/20">
        Disponible para trabajar
      </span>
      <h1 class="text-4xl font-extrabold tracking-tight sm:text-6xl lg:text-7xl">
        Hola, soy <br class="block md:hidden" />
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-white to-gray-500">
          Jeysson Sánchez
        </span>
      </h1>
      <h2 class="text-xl font-medium text-gray-400 sm:text-2xl h-8" id="typewriter">
        <!-- Text injected by JS -->
      </h2>
    </div>

    <p class="max-w-lg text-lg text-gray-400">
      Desarrollador Frontend & Mobile especializado en <span class="text-white font-semibold">Angular</span> y <span class="text-white font-semibold">Flutter</span>.
      Creo aplicaciones web y móviles rápidas, fluidas y escalables, con un sólido conocimiento en Backend.
    </p>

    <div class="flex flex-wrap items-center gap-4">
      <a 
        href="#projects" 
        class="group relative inline-flex items-center gap-2 overflow-hidden rounded-full bg-white px-8 py-3 text-sm font-bold text-black transition-transform hover:scale-105"
      >
        <span>Ver Proyectos</span>
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="transition-transform group-hover:translate-y-1"><path d="M12 5v14"/><path d="m19 12-7 7-7-7"/></svg>
      </a>
      
      <a 
        href="/documents/CV_Jeysson.pdf" 
        class="inline-flex items-center gap-2 rounded-full border border-white/20 bg-white/5 px-8 py-3 text-sm font-bold text-white backdrop-blur-sm transition-colors hover:bg-white/10 hover:border-white/40"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" x2="12" y1="15" y2="3"/></svg>
        Descargar CV
      </a>
    </div>
  </div>

  <div class="relative flex items-center justify-center">
    <!-- Abstract Background Blob -->
    <div class="absolute -z-10 h-[300px] w-[300px] animate-blob rounded-full bg-accent/30 blur-3xl filter md:h-[500px] md:w-[500px]"></div>
    
    <div class="relative h-64 w-64 overflow-hidden rounded-2xl border-2 border-white/10 bg-gray-900/50 shadow-2xl backdrop-blur-sm transition-transform hover:scale-105 md:h-96 md:w-80 rotate-3 hover:rotate-0 duration-300">
      <img 
        src="/images/perfil.webp" 
        alt="Jeysson Sánchez" 
        class="h-full w-full object-cover opacity-80 transition-opacity hover:opacity-100"
      />
    </div>
  </div>
</section>

<script>
  const titles = ["Frontend Developer", "Mobile Developer", "Backend Knowledge"];
  const element = document.getElementById("typewriter");
  
  let i = 0;
  let j = 0;
  let isDeleting = false;
  let loop = true;

  function type() {
    if (!element) return;
    const current = titles[i % titles.length];
    const fullTxt = current;

    if (isDeleting) {
      element.textContent = fullTxt.substring(0, j - 1);
      j--;
    } else {
      element.textContent = fullTxt.substring(0, j + 1);
      j++;
    }

    let typeSpeed = 100;

    if (isDeleting) {
      typeSpeed /= 2;
    }

    if (!isDeleting && j === fullTxt.length) {
      isDeleting = true;
      typeSpeed = 2000; // Pause at end
    } else if (isDeleting && j === 0) {
      isDeleting = false;
      i++;
      typeSpeed = 500; // Pause before new word
    }

    if (loop) {
      setTimeout(type, typeSpeed);
    }
  }

  type();
</script>
